import { Profile } from '../components/Profile';
import Styled from './Page.styles';

const ProfilePage = () => {
  return (
    <Styled.Wrapper>
      <Profile />
    </Styled.Wrapper>
  );
};

export default ProfilePage;
