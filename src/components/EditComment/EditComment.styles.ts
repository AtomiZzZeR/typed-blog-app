import styled from 'styled-components';

const Form = styled.form`
  margin: 0px 400px 0px 0px;
`;

const Input = styled.input``;

const Button = styled.button``;

const StyledEditComment = {
  Form,
  Input,
  Button,
};

export default StyledEditComment;
