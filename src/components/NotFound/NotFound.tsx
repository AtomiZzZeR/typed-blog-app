import React from 'react';
import Styled from './NotFound.styles';

const NotFound = () => {
  return (
    <Styled.Wrapper>Error! Page not found! (404) Go to home page.</Styled.Wrapper>
  );
};

export default NotFound;
